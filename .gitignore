node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Environment files with secrets
.env
.env.*

# Chromium cache and crash reports (contain secrets)
.config/
.local/

# Attached assets (contain API keys in text files)
attached_assets/

# Database backups (too large for git)
*.sql
neon_backup.sql